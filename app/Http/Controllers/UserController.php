<?php

namespace App\Http\Controllers;

use App\Models\User;
use Illuminate\Http\Request;
// Hapus use statement yang tidak perlu seperti View, RedirectResponse, Hash, dll.

class UserController extends Controller
{
    // Kosongkan semua metode (index, create, store, edit, update, destroy) dari sini.
    // Controller ini tidak lagi bertanggung jawab untuk manajemen pengguna.
}